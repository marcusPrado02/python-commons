"""Adapters – concrete implementations (optional extras).

Install what you need::

    pip install mp-commons[fastapi,sqlalchemy,redis,otel,kafka]

Each sub-package is independently importable and has its own dependencies.
"""
